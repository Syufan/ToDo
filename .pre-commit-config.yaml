---
# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks

repos:
  - repo: meta
    hooks:
      - id: check-useless-excludes

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-added-large-files
        args: [--maxkb=1000]
      - id: check-merge-conflict
      - id: detect-aws-credentials
        args:
            - --allow-missing-credentials
      - id: detect-private-key
      - id: debug-statements
      - id: check-yaml
      - id: check-xml

  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v3.2.5
    hooks:
      - id: prettier
        additional_dependencies: [prettier, prettier-plugin-tailwindcss]
        files: ^client/(public|src)/.*\.(css|html|js|jsx|json|md|scss|ts|tsx|yaml|yml)$

  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.41.0
    hooks:
      - id: markdownlint
        files: ^(README|doc/).*\.md$

  - repo: local
    hooks:
      - id: client-eslint
        name: client eslint
        entry: bash
        args:
          - -lc
          - cd client && npx --no-install eslint src --ext .ts,.tsx,.js,.jsx
        language: system
        pass_filenames: false
        files: ^client/src/.*\.(js|jsx|ts|tsx)$

      - id: dotnet-format
        name: dotnet format
        entry: bash
        args:
          - -lc
          - cd server && dotnet format --verify-no-changes
        language: system
        pass_filenames: false
        files: ^server/.*\.(cs|csproj)$
